#!/usr/bin/env perl 

use strict;
use warnings;
use utf8;
use feature 'state';

use Data::Dumper qw(Dumper);

#sub a {
  #state $b = 1;
  #print qq{$b} . "\n";
  #$b++;
#}

#foreach my $i (1 .. 10 ) {
    #a();
#}
#my @a = ( 1 .. 10 );
#my @b = @a;

#print Dumper(\@b) . "\n";

#my $a = 1;
#my $b = 2;

#( print  'c' if $a ) || $b;
#sub a { 
    #my $r = shift;
    #my $b = $r->{a} // 1;
    #print qq{$b} . "\n";
#}

#a({ a  => 0 });

my @a = ();
if ($a[0]) {
    # body...
}
